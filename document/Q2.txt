场景导入阶段进行的判断是某楔边成为UTD楔边的最基本的两个条件：1.两个相互接触的面元。2.两个面元张成的楔边角度（两面元的法向指向同一空间）取值在3°~177°（文献理论值）。这是第一步在场景导入时的预处理，后续读取到了Tx以及Rx进行每对绕射的寻径时，之前预处理的棱边是否可以作为楔边还要根据TxRx与该楔边的相对位置确定该边是否可以作为UTD楔边。即如果Tx-Rx都在“墙外”即在183°~357°区间内则根据UTD理论可以以该边为绕射边发生绕射。